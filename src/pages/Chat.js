import React from 'react'
import Chat from '@/components/Chat/Chat'

const message = ({ users }) => {
    return (    
        <div>
        
        <Chat/>


        </div>
    );
}

export default message;

